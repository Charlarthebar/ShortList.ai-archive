# Job Database API Keys
# Copy this file to .env and fill in your credentials
# DO NOT commit .env to git (it should be in .gitignore)

# Adzuna API - Get from https://developer.adzuna.com
ADZUNA_APP_ID=your_app_id_here
ADZUNA_APP_KEY=your_app_key_here

# USAJOBS API - Get from https://developer.usajobs.gov
USAJOBS_API_KEY=your_api_key_here
USAJOBS_EMAIL=your_email_here

# Optional: Google Places API - For discovering local businesses
# GOOGLE_PLACES_API_KEY=your_key_here

# Optional: Database settings (defaults work for SQLite test)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=jobs_db
# DB_USER=postgres
# DB_PASSWORD=your_password

# ============================================================================
# EMAIL SERVICE CONFIGURATION
# ============================================================================

# Email provider: 'smtp', 'sendgrid', or 'console' (for testing/development)
EMAIL_PROVIDER=console

# Sender configuration
EMAIL_FROM_ADDRESS=noreply@shortlist.ai
EMAIL_FROM_NAME=ShortList

# Frontend URL (for email links like password reset)
FRONTEND_URL=http://localhost:3000

# SMTP settings (required if EMAIL_PROVIDER=smtp)
# For Gmail, use smtp.gmail.com and an App Password (not your regular password)
# To create an App Password: Google Account > Security > 2-Step Verification > App passwords
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# SendGrid settings (required if EMAIL_PROVIDER=sendgrid)
# Get your API key from https://app.sendgrid.com/settings/api_keys
# SENDGRID_API_KEY=SG.your_api_key_here

# ============================================================================
# JWT CONFIGURATION
# ============================================================================

# Secret key for JWT tokens (generate with: python -c "import secrets; print(secrets.token_hex(32))")
# If not set, a random key is generated on each restart (tokens won't persist)
# SECRET_KEY=your_secret_key_here

# ============================================================================
# AI SCORING (Optional)
# ============================================================================

# OpenAI API key for AI-powered candidate scoring
# Get your API key from https://platform.openai.com/api-keys
# If not set, fallback scoring based on objective criteria is used
# OPENAI_API_KEY=sk-your_api_key_here
